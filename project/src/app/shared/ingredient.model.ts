export class Ingredients{
    // public name: string;       //we have an optimised approach for this
    // public amount: number;

    // constructor(name:string,amount:number){
    //     this.name=name;
    //     this.amount=amount;       
    // }

    constructor(public name:string,public amount:number){   //this will do the same work as we did above in the back
    }
}